#include <windows.h>

int WINAPI WinMain(
    HINSTANCE hInstance, 
    HINSTANCE hPrevInstance, 
    PWSTR pCmdLine, 
    int nCmdShow) 
{
    // Register window class
    // Create the main window
    // Show the window
    // Run the message loop
    return 0;
}